import path from 'path';
import testData from '../test-data/test-data.json';

export const base_url = testData.baseUrl;

export const testFilePath = path.resolve(__dirname,'../test-data/test-data.json');
